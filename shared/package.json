{
  "name": "@mycelium/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contract code (types, trust, env-math, fingerprint, embeddings) imported by mcp and web.",
  "exports": {
    ".": "./src/index.ts",
    "./embed": "./src/embed.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
