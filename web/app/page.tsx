// PLACEHOLDER homepage — Person 2 (Rithvik) builds the real scroll story here. Safe to overwrite.
export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 48 }}>
      <h1>🍄 Mycelium</h1>
      <p>The collective intelligence layer for AI agents. (Frontend in progress.)</p>
      <p>
        Backend API is live at <code>/api/skills</code>, <code>/api/stats</code>,{" "}
        <code>/api/trails</code>, and <code>POST /api/search</code>.
      </p>
    </main>
  );
}
